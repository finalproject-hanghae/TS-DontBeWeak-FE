export type weekData = {
  usedAt: string;
  dayOfWeekValue: number;
  productName: string;
  customColor: string;
  done: string;
};

export type weekList = weekData[];

export type startAndEndDate = {
  startDate: string;
  endDate: string;
};
